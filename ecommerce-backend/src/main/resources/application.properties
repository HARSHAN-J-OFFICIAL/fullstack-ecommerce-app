spring.application.name=ecommerce-backend

# DATABASE CONFIGURATION
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db
spring.datasource.username=root
spring.datasource.password=root

# RAZORPAY CONFIGURATION
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_TGsQHEmdYBiWap}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:WDedD4GbuVBD3fcBis8hU5bk}

# JPA CONFIGURATION
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SERVER PORT
server.port=8080

# MAIL CONFIGURATION
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:demo@ecommerce.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:demopassword}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
admin.notification.email=${ADMIN_NOTIFICATION_EMAIL:admin@ecommerce.com}